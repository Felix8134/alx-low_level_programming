#include "main.h"

/**
 * mul -checks for digit (0 through 9)
 * @c: a -variable
 * @c: b -variable
 * Return: Always 0 (Success)
 */

int mul(int a, int b)

{
	int mul;

	mul = a * b;
	return (mul);
}
